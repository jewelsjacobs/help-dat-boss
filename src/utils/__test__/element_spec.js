const expect = chai.expect;
const should = chai.should();
import { setElementProperties } from '../element';

describe('setElementProperties', () => {
  it('should do something', () => {
    expect(5).to.not.equal(4);
  });
});
